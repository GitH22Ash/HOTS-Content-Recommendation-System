# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies
node_modules

# Build output
dist
dist-ssr

# Environment variables (NEVER commit these)
.env
.env.local
.env.*.local

# Backend (Python)
backend/venv
backend/venv312
backend/__pycache__
backend/*.pyc
backend/preprocess_model.py

# NOTE: backend/*.pkl files are intentionally TRACKED (needed for Render deployment)
# They contain ML model weights only — no credentials or sensitive data

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
